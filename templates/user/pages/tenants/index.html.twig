{% extends 'user/pages/tenants/template.html.twig' %}

{% set page_title %}Liste des locataires{% endset %}

{% block main_content %}
    <div id="list-tenants" data-donnees="{{ data }}" data-negotiators="{{ negotiators }}"
         data-society-id="{{ user.society.id }}" data-agency-id="{{ user.agency.id }}">
        {% include "dashboard/layout/components/spinner.html.twig" %}
    </div>
{% endblock %}