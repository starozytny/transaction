{% extends 'user/layout/layout.html.twig' %}

{% set submenu = [
    { 'label': "Liste des locataires", 'path': path('user_tenants') },
] %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('user_tenant') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('user_tenant') }}
{% endblock %}