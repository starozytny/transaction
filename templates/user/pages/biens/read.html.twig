{% extends 'user/pages/biens/template.html.twig' %}

{% set page_title %}{{ elem.shortname }}{% endset %}

{% block main_content %}
    <div id="read-bien"
         data-elem="{{ data }}"
         data-tenants="{{ tenants }}"
         data-rooms="{{ rooms }}"
         data-photos="{{ photos }}"
         data-suivis="{{ suivis }}"
         data-negotiators="{{ negotiators }}"
         data-visits="{{ visits }}"
    >
        {% include "dashboard/layout/components/spinner.html.twig" %}
    </div>
{% endblock %}