{% extends 'user/pages/profil/template.html.twig' %}

{% set page_title %}Modifier {{ elem.lastname }} {{ elem.firstname }}{% endset %}
{% set submenuActive = "Mon profil" %}

{% block main_content %}
    <div id="user-update"
         data-donnees="{{ donnees }}"
         data-society-id="{{ elem.society.id }}"
         data-agency-id="{{ elem.agencyId }}"
         data-negotiators="{{ negotiators }}"
    >
        {% include "dashboard/layout/components/spinner.html.twig" %}
    </div>
{% endblock %}
